import {StyleSheet} from 'react-native'

export default StyleSheet.create({
  center: {
    justifyContent: 'center',
    alignItems: 'center'
  },

  slide: {
    width: 250,
    height: 100
  }
});
